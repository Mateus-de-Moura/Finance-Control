@model Finance.Control.webApp.ViewModel.AppUserViewModel

@{
    ViewData["Title"] = "Atualização de Usuários";
}


<form asp-action="Update" method="post">

    <div class="col-12 mb-2 mb-xl-3 d-flex">
        <div class="col-auto">
            <h5 >Atualização de Usuários</h5>
        </div>

        <div class="col-auto ms-auto text-end mt-1">
            <a class="btn btn-outline-secondary" asp-action="Index">
                Voltar
            </a>
        </div>
    </div>

    <partial name="Partials/_Form"  model="@Model"/>
</form>